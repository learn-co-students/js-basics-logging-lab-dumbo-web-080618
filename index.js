const driver = 'bob';
console.log('this code was called')//invokes console.log() with the string "this code was called"
console.log(driver) //calling the above variable
console.log(driver, 'is the driver variable name') //invokes the console.log with the `driver` variable as the first argument and `is the driver variable name` as the second argument
